const React = require('react')
const Default = require('./Default')

function Home (html) {
return (
    <Default>
        <main>
            <h1>Home Page</h1>
        </main>
    </Default>
)
}

module.exports = Home